---
title: Paging
n: 3
date: "2026-09-05"
summary: "Understand how paging works in xv6"
authors:
  - name: "Veena"
    matrix: "@veena.ramadas:matrix.iiit.ac.in"
---

This homework covers how paging and memory allocation is done in xv6.

You can go through the following files:

* `kernel/memlayout.h`
* `kernel/vm.c`
* `kernel/kalloc.c`
* `kernel/riscv.h`
* `kernel/exec.c`
* `kernel/sysproc.c` (`sbrk()`)
* `kernel/trap.c` (`usertrap()`)
* `kernel/vm.c` (`vmfault()`)

Chapters 3 and 5 of [the book](https://pdos.csail.mit.edu/6.828/2025/xv6/book-riscv-rev5.pdf) (*Page Tables* and *Page Faults*) will help, especially sections 3.2, 3.6, 3.7 and 5.1.

xv6 uses page tables to give each process its own virtual address space, translating virtual addresses to physical memory while enforcing access through PTE permissions. The kernel builds and manages these mappings using functions such as `walk()` and `mappages()`, while `kalloc()`/`kfree()` manage the physical pages backing them; page faults allow the kernel to handle invalid or unauthorized accesses and allocate memory lazily when required.

#### Questions

1. In `loadseg()`, after obtaining the physical address `pa`, what allows `readi()` to write directly to that address? Which line of code makes the physical address usable as a kernel virtual address?

## DIY: understanding lazy allocation

`sbrk()` syscall grows a process's address space. xv6 has two ways of handling this growth: (i) eager allocation, physical memory is allocated and mapped when the address space is expanded; (ii) lazy allocation, only the process's address-space size is increased, a physical page is accessed when the memory is first accessed.

### Task

Write a user program that grows its address space by one byte by calling the `sbrk` syscall. Run the program using both `SBRK_EAGER` and `SBRK_LAZY`.

Inspect the process's page table before and after the call in each case. For the lazy allocation case, also access the newly allocated memory and inspect the table again.

Use the page-table information to determine how much memory is allocated in each case and how the PTE for the newly allocated memory changes. The `vmprint()` function in `kernel/vm.c` can be used to print a process's page table. You can call it for the current process using `myproc()->pagetable`. Printing the page table before and after each `sbrk()` call will be helpful.

### Running your program

1. Create a user program `user/test_sbrk` that calls `sbrk(1, SBRK_EAGER)` and `sbrk(1, SBRK_LAZY)`.
2. Add `$U/_test_sbrk` to `UPROGS` in Makefile.
3. Run your program in QEMU:
   ```bash
   make qemu
   ```
4. Inspect the process's page table before and after each `sbrk(1)` call, and after accessing the lazily allocated memory.

